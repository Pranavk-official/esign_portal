# Local Development Configuration

# Backend URL — server-side only (NOT exposed to the browser bundle).
# Requests from the browser go to /api/* which this Next.js server proxies to BACKEND_URL.
BACKEND_URL="http://10.5.141.53"
