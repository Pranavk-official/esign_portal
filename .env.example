# Backend API base URL (server-side only - no trailing slash).
# This is proxied through /api/* so the backend IP is never exposed to the browser.
BACKEND_URL="https://your-backend-url-or-ip"

# For VMs with self-signed TLS certificates, add this to .env.local (NOT production):
# NODE_TLS_REJECT_UNAUTHORIZED=0